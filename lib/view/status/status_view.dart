import 'package:flutter/material.dart';

class StatusView extends StatelessWidget {

  static const routeName = 'status-view';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Status View'),
    );
  }
}
