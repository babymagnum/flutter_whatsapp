import 'package:flutter/material.dart';

class CameraView extends StatelessWidget {

  static const routeName = 'camera-view';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Camera View'),
    );
  }
}
