import 'package:flutter/material.dart';

class CallsView extends StatelessWidget {

  static const routeName = 'calls-view';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Calls View'),
    );
  }
}
